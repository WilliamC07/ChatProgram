#define MAX_LENGTH_MESSAGE 256  // Includes end of string character, so there is 255 working characters
#define MAX_LENGTH_COMMAND 8  // Includes end of string character, so there are 7 working characters

void handle_input(char input);